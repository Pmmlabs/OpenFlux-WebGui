# OpenFlux

[English](README.md) | **Русский**

Исследовательский инструмент сетевого стека: TCP-туннель поверх подключаемых
скрытых транспортов вместо прямого соединения с VPN/прокси-сервером.

# Отказ от ответственности

Автор OpenFlux **не призывает** использовать данный проект для обхода
блокировок или нарушения правил каких-либо платформ, а также **не несёт
ответственности** за финальные сценарии использования утилиты пользователями
в реальной жизни или сети Интернет. Любые специфические технические
особенности приложения - не более чем **архитектурное совпадение**, созданное
**без какого-либо умысла**.

Проект является **полностью некоммерческим**, не содержит **платных функций,
скрытых подписок или коммерческой выгоды**.

Автор **не несёт ответственности** за форки, модификации и производные
версии OpenFlux, созданные третьими лицами. Любые изменения, добавленные
в форк, являются ответственностью его автора.

Автор **не несёт ответственности** за:

- Любое использование OpenFlux третьими лицами
- Последствия, вызванные использованием форков и модификаций
- Ущерб, возникший в результате работы производных версий
- Нарушения, совершённые с использованием форков

Оригинальный код предоставляется **как есть** («as is»), **без каких-либо
гарантий**.

## Описание

Форк [p1neappleXpress/OpenFlux](https://github.com/p1neappleXpress/OpenFlux),
в паре с [Android-клиентом](https://github.com/Kingofthedivanich/OpenFluxAndroid).
Поверх апстрима этот форк добавляет: опциональное Noise-шифрование (плюс режим
«только PSK» AES-256-GCM, совместимый со старыми клиентами
`--encryption-key-file`), мультиклиентскую веб-панель управления
(`--role=exit-panel`) с опциональным Telegram-ботом, подключение клиента
одним сканом QR, автогенерацию документов Yandex, multi-stream (несколько
документов — один туннель), SSRF-деннилист на выходной ноде и большой список
исправлений корректности/надёжности в транспортах и на выходной ноде —
детали в истории коммитов.

## Как это работает

- Клиент и выходная нода общаются через скрытый транспорт (Yandex.Docs,
  Yandex Volga, MAX/OneMe, Cups.online, Mail.ru Docs) вместо прямого
  соединения.
- Трафик склеивается, сжимается zstd и опционально шифруется: Noise NKpsk0
  (X25519 + AES-256-GCM, ротация сессионных ключей) с ключами, либо
  AES-256-GCM «только PSK» (без хендшейка) с общим `--psk-file`.
- Выходная нода пересылает реальный трафик одним из двух бэкендов:
  `--mode=l3` (raw SNAT/DNAT, Linux + root, быстрее всего) или `--mode=l4`
  (gVisor-прокси в userspace, любая ОС, без root).
- `--role=exit-panel` запускает много клиентов в одном процессе за небольшой
  веб-панелью (добавление/удаление, статус, QR-коды, опциональный
  Telegram-бот) — вместо одного процесса выходной ноды на клиента.

## Гайд по установке

```bash
go build -o openflux ./cmd/openflux
```

Выходная нода (выбери один вариант):

```bash
# l3 — Linux, root, быстрее всего
sudo ./openflux --role=exit --mode=l3 --transport=yandex --url="ССЫЛКА_НА_ДОКУМЕНТ"

# l4 — любая ОС, без root
./openflux --role=exit --mode=l4 --transport=yandex --url="ССЫЛКА_НА_ДОКУМЕНТ"
```

Клиент:

```bash
./openflux --role=client --transport=yandex --url="ССЫЛКА_НА_ДОКУМЕНТ"
```

На macOS по умолчанию используется интерфейс utun; на остальных платформах —
SOCKS5-прокси на `127.0.0.1:1080` (`--socks5=<адрес>`, чтобы изменить).

Мультиклиентская выходная нода с веб-панелью вместо процесса на каждого
клиента:

```bash
./openflux --role=exit-panel \
    --panel-addr=127.0.0.1:8088 --panel-user=admin --panel-pass=CHANGE_ME
```

Управляй клиентами (добавление/удаление, QR-коды, Telegram-бот, автогенерация
документов Yandex) из веб-интерфейса. Держи `--panel-addr` на loopback и
заходи через SSH-туннель (`ssh -L 8088:127.0.0.1:8088 user@host`). Прямо на
VPS — [`deploy/menu.sh`](deploy/menu.sh) (`openflux-ctl`), консоль в стиле
3x-ui для обновлений, клиентов и бота, без ручного редактирования
systemd-юнита.

Шифрование опционально: без ключей туннель идёт в открытом виде
(`--allow-plaintext` лишь подавляет предупреждение). Чтобы включить — либо
раздай один `--psk-file` обеим сторонам (режим «только PSK», AES-256-GCM,
без файлов ключей — его же понимают старые клиенты `--encryption-key-file`),
либо пусть нода напечатает публичный ключ при первом запуске и передай его
клиентам через `--peer-key` (Noise NKpsk0). `--panel-key-file` у панели тоже
опционален: без него клиенты панели — plaintext или «только PSK».

Готовые клиенты: [Android](https://github.com/Kingofthedivanich/OpenFluxAndroid/releases)
(клиент этого форка). [iOS](https://github.com/Kingofthedivanich/OpenFluxiOS) —
тоже клиент этого форка (пока только исходники, собирать самому);
[TestFlight апстрима](https://testflight.apple.com/join/BwnAcdus) тоже
работает, но не в паре с шифрованием этого форка — идёт в открытом виде.
macOS/Linux/Windows: собирай из исходников выше.

Полный список флагов: `./openflux --help`.

## Лицензия

GNU General Public License v3.0 or later. Полный текст - в файле LICENSE.

Лицензии третьих сторон - в файле [NOTICE](NOTICE).
